window.onload=function(){
    let x = prompt("숫자를 입력하세요");
    let y = prompt("숫자를 입력하세요");
    if(x>y){
        document.write("small");
    }
        else{
            document.write("big");
        }
    }
